defmodule DundaWeb.ResaleControllerTest do
  use DundaWeb.ConnCase

  describe "POST /api/resale/listings" do
    test "creates a new listing", %{conn: conn} do
      # assert true
    end
  end

  describe "POST /api/resale/listings/:id/buy" do
    test "purchases a listing, transfers ticket, and marks sold", %{conn: conn} do
      # assert true
    end
  end
end
