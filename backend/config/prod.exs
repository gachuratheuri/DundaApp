import Config

# Production runtime values (DB URLs, encryption keys, Daraja credentials) are
# loaded from the environment in `config/runtime.exs`. This file holds only
# compile-time production defaults.

config :logger, level: :info
